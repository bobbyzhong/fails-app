<html>
    <head>
        {{> header}}
    </head>
    <body>
        {{> menu}}
        <div class="container">
            <h1>Latest Posts</h1>
            <div class="list-group">
                {{#posts}}

                <div class="d-flex w-100 justify-content-between">
                    <h5 class="mb-1">{{ title }}</h5>
                </div>
                <p class="mb-1">{{ body }}</p>

                {{/posts}}
            </div>
        </div>

        {{> footer}}
    </body>
</html>
